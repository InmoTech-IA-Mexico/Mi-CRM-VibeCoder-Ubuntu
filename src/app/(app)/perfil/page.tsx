import { PantallaPerfil } from "./_components/pantalla-perfil";

// Perfil / Ajustes (JUA-120): editar datos personales, cambiar contraseña, accesos
// de administrador (solo Marta) y cerrar sesión (JUA-11).
export default function Page() {
  return <PantallaPerfil />;
}
